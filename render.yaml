services:
  - type: web
    name: newsbulletin
    env: node
    buildCommand: npm install && npm run build && npm run migrate && npm run seed
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false 